import 'package:flutter/material.dart';

class OrderCancellerScreen extends StatelessWidget {
  const OrderCancellerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OrderCancellerScreen'),
      ),
    );
  }
}
